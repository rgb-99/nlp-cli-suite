[project]
name = "nlp-cli-suite"
version = "0.1.0"
requires-python = ">=3.8"
dependencies = [
    "nlp-engine-yuvraj>=0.1.0",
    "hf-inference-benchmark>=0.1.3",
    "click>=8.0"
]

[project.scripts]
nlp-tool = "nlp_tool.cli:main"
